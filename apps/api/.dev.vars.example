# 本地开发用的环境变量示例 — 复制为 .dev.vars 并填入真值
# .dev.vars 在 .gitignore 中,不会进 git
# 生产环境的真值通过 `wrangler secret put` 写入 Workers 后台

LLM_API_KEY="sk-..."          # MiniMax API key
PASSCODE_HASH="$2b$10$..."    # 口令的 bcrypt hash
JWT_SECRET="random-string-min-32-chars"
